apiVersion: v1
kind: Service
metadata:
  name: properties-api-server
  namespace: fleet-system
  labels:
    app: properties-api-server
spec:
  type: ClusterIP
  ports:
    - port: 443
      targetPort: 6443
      protocol: TCP
      name: https
  selector:
    app: properties-api-server
